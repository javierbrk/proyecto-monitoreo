// Test stubs for Grafana manager - no Arduino dependency
#include <unity.h>

void testSendDataGrafana() {
    // Stub test - actual HTTP sending requires hardware
    // This validates the test infrastructure works
    TEST_ASSERT_TRUE(true);
}
